GhanaTopUp Mobile (scaffold)

Run locally (after installing dependencies at repo root):

```bash
cd apps/mobile
npm install
npm run start
```

This is a minimal Expo scaffold that imports shared Zod schemas from `@ghanatopup/shared`.
