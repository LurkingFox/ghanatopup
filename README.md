# GhanaTopUp — Monorepo (scaffold)

This repository contains scaffolding for the GhanaTopUp MVP described in `GhanaTopUp_Handover.md`.

Structure:

- apps/api — Fastify backend (TypeScript)
- apps/mobile — React Native + Expo app (placeholder)
- packages/shared — shared Zod schemas and types

Next steps:

- Install dependencies in each package
- Implement authentication, DB schema, and integrations

